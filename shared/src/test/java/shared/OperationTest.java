package shared;

import static org.junit.jupiter.api.Assertions.*;

import org.junit.jupiter.api.Test;

public class OperationTest {
  @Test
  public void test_initOP() {
    //  InitOperation io = new InitOperation(1, 2);
    // assertEquals(1, io.getDest());
    //assertEquals(2, io.getNum());
    //assertEquals(0, io.getType());
  }

}
